# Sesión 2026-05-31 — Sprints 2, 4 y 5 (ejecución de la auditoría)

## Qué se hizo
Auditoría de 5 agentes (mañana) → plan maestro → **ejecución y verificación** de los sprints núcleo.

### Sprint 2 — "Que lo que existe funcione de verdad" (TODOS los 🔴 + nuevos)
- **data.jsx:** `updatedAt` idempotente en lectura (N2); materialización de ocurrencias recurrentes
  para que editar/togglear persista (B3); limpieza de overrides huérfanos con flag `_edited` (N3);
  `applyRoutine` salta horas ocupadas (B10); `loWrite` avisa por QuotaExceeded; habits/inbox con
  `updatedAt`; `loFindTaskByQuery` (NLP no borra la tarea equivocada); cadencia de hábitos
  (`loHabitScheduledOn`, streak respeta cadencia); `updateHabit`; backup de migración legacy no-array;
  regex de diacríticos robusta.
- **screens.jsx:** Create guarda fecha absoluta (B14 ⭐), reminder/recur (B2), pasos editables (B1),
  `loUid`+`durationMin` (HU-01/B15), sin fila Calendario muerta; Stats con datos reales (B7);
  NotifSettingsRow placebo borrado (B5); SettingsRow con `onClick` real (B8); button-in-button
  arreglado (B6); badge PRO falso y lupa de Agenda removidos; Detail totalmente editable (B16).
- **notify.jsx:** `useTaskNotifications` lee tareas reales + `setInterval` 60s (N1).
- **extras.jsx:** Focus Mode ✓ completa + guard NaN + toast al llegar a 0 (B4); botones muertos de
  Rutinas removidos (B8); HabitCreateSheet con cadencia + edición/borrado; long-press para editar;
  `flame`→`fire`.
- **app.jsx:** `completeFocus`, `scheduleInbox` (hueco libre, B12), `openHabitEdit` cableados en
  PrototypeFrame y TabletApp; helper `loFreeSlotStart`.

### Sprint 4 — Diseño (anti-IA)
Grano de película global + Fraunces en todos los números grandes + `prefers-reduced-motion`.

### Sprint 5 — Diferenciador
Heatmap de consistencia por hábito (18 semanas) + stats Racha/Mejor/% 30 días en edición.

## Verificación
- `verify-sprint2.js` → **13/13 PASS, 0 console errors** (B2,B3,B14,B10,N2,N3,HU-01,B15,hábitos,NLP).
- `verify-heatmap.js` → heatmap + stats renderizan, 0 errores.
- `smoke.js`/`flow.js` → 0 errores reales (solo 404 de favicon).
- `node check.js` OK · `python3 rebuild.py` ejecutado (src → index.html).

## Pendiente
S3 (React prod + matar Babel + export/import + CSP), S6 (WeekScreen real, rutinas custom, all-day,
deadlines), S7 (modo pareja + sync), S8 (AI real). Detalle en `docs/scrum/PLAN-MAESTRO.md`.

## Nota
No se hizo commit (regla: commitear solo cuando Tomás lo pida). Cambios en working tree de `src/*.jsx`
+ `index.html` reconstruido. Para desplegar: `python3 rebuild.py` (ya hecho) → deploy Cloudflare.
